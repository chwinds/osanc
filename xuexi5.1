#include <stdio.h>
int main()
{
	//数组
	int x[10] = {0};

	//赋值
	int i = 0;
	for  (i = 0; i < 10; i++)
	{

		scanf_s("%d", &x[i]);
	}

	//比较
	int max = x[0];
	for (i = 1; i < 10;i++)
	{
		if (x[i] > max) 
		{
			max = x[i];
		}

	}

	//输出
	printf_s("最大值为%d", max);





	return 0;
}
